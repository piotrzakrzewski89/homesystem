app:
    resource: '@AppBundle/Controller/'
    type: annotation

app_bundle_route:
   path:     /
   defaults: { _controller: AppBundle:Main:ajax }

fos_user:
    resource: "@FOSUserBundle/Resources/config/routing/all.xml"

fos_user_group:
    resource: "@FOSUserBundle/Resources/config/routing/group.xml"
    prefix: /group
